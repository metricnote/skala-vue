# SKALA Vue 프로젝트 개발 기록

## 1. 결과물에서 중점적으로 구현한 부분

### 학습 단계와 최종 서비스의 공존

- Day 1~Day 2-2 결과는 과제 1~3 접이식 학습 기록으로 보존했습니다.
- Day 3 이후 결과는 별도 과제 화면이 아니라 실제 날씨 생활 서비스 형태로 발전시켰습니다.
- `practice`와 `exercise`를 분리하고 제출 기본 모드는 `exercise`로 유지했습니다.

### 실제 데이터와 사용자 경험

- OpenWeather Current Weather API로 서울, 수원, 부산의 실시간 날씨를 제공합니다.
- Air Pollution API로 AQI, PM2.5, PM10, 오존과 행동 안내를 추가했습니다.
- TheMealDB API로 실제 음식 이름, 이미지, 분류와 레시피 링크를 제공합니다.
- 날씨에 따라 음식 카테고리와 코디 아이템을 다르게 추천합니다.
- 시베리아, 아마존, 알래스카, 사하라 사막은 극한 날씨를 확인하는 가상 시뮬레이션 지역으로 구분했습니다.

### 시각적 완성도

- Element Plus의 카드, 태그, 스위치, 알림, 스켈레톤, 설명 컴포넌트를 사용했습니다.
- SVG `feGaussianBlur`와 `feColorMatrix`를 이용해 서로 붙는 젤리 효과를 구현했습니다.
- `backdrop-filter`, 내부 하이라이트와 프리즘 광택 애니메이션으로 리퀴드 글라스 질감을 구성했습니다.
- 필터가 글자를 흐리지 않도록 장식 레이어와 콘텐츠 레이어를 분리하고 `clip-path`, `isolation`, `z-index`를 사용했습니다.

## 2. 개발 중 발생한 문제와 해결

### HMR에서 새 컴포넌트를 찾지 못한 오류

- 증상: `WeatherHomeView.vue`가 `WeatherRecommendations.vue`를 찾지 못한다는 Vite 오류가 발생했습니다.
- 원인: 여러 파일을 복사하는 도중 HMR이 View 변경을 먼저 감지했고 새 컴포넌트 복사는 아직 끝나지 않았습니다.
- 해결: 파일 존재 여부와 import 경로를 확인하고 Vite 서버를 재시작했습니다. 새 서버에서 관련 모듈이 모두 HTTP 200으로 로드되는 것을 확인했습니다.

### Element Plus 기본 스타일과 사용자 CSS 충돌

- 증상: 헤더 높이가 60px로 고정되고 사용자 여백이 적용되지 않았습니다.
- 원인: Element Plus CSS 로드 순서와 `el-header` 기본 스타일 우선순위가 사용자 CSS보다 높았습니다.
- 해결: Element Plus CSS 다음에 `exercise.css`를 불러오고 헤더 높이와 여백을 명시적으로 재정의했습니다.

### Vue 기본 템플릿의 2열 레이아웃

- 증상: 대시보드가 화면 왼쪽 절반에만 표시되었습니다.
- 원인: 초기 `main.css`의 데스크톱 `#app` 그리드와 body flex 규칙이 남아 있었습니다.
- 해결: 최종 앱에서 `#app`을 전체 너비 block 레이아웃으로 덮어썼습니다.

### GitHub Pages 하위 경로 문제

- 위험: `/assets/...` 절대 경로를 사용하면 `/skala-vue/`에 배포했을 때 이미지와 번들이 404가 됩니다.
- 해결: Vite `base: '/skala-vue/'`, Router `import.meta.env.BASE_URL`, 공개 이미지 경로에도 `BASE_URL`을 적용했습니다.

### API 키 보안

- 문제: 교재 practice 예제에 32자리 API 키가 하드코딩되어 있었습니다.
- 해결: 모든 소스에서 키를 제거하고 `VITE_OPENWEATHER_API_KEY` 환경변수만 사용하도록 통일했습니다.
- 주의: 프론트엔드 환경변수는 Git 유출을 막지만 빌드된 브라우저 코드에서는 확인할 수 있습니다. 실제 상용 서비스라면 백엔드 프록시가 필요합니다.

### 외부 API 부분 실패

- 상황: 날씨, 대기질, 음식 API는 서로 다른 서버이므로 일부만 실패할 수 있습니다.
- 해결: 추가 API를 독립 상태로 관리하고 `Promise.allSettled`를 사용했습니다. 음식 API 실패 시 내부 날씨 추천을 표시하고, 대기질 실패 시 경고만 보여 주며 상세 날씨는 유지합니다.

## 3. 특별한 설계 상황

- 실제 지역은 OpenWeather의 실시간 날씨와 좌표 기반 대기질을 사용합니다.
- 가상 지역은 실시간 관측으로 오해하지 않도록 `SIMULATION` 배지를 표시하고 대기질 API 호출을 생략합니다.
- TheMealDB는 교육용 무료 키 `1`을 사용하며 별도의 비밀키가 필요하지 않습니다.
- 음식 추천은 비·눈·저온·고온 상태를 Pasta, Beef, Seafood, Vegetarian 카테고리로 연결합니다.
- 사용자 모션 최소화 설정에서는 젤리와 유리 굴절 애니메이션을 정지합니다.

## 4. 배포 전 검증 기준

- `npm run lint`: 오류 0건
- `npm run build`: GitHub Pages base 경로로 빌드 성공
- 소스에서 32자리 API 키 검색 결과 0건
- `.env.local`, `node_modules`, `dist`가 Git에 포함되지 않는지 확인
- 메인, About, 상세, 404, 쿼리 검색, 섭씨·화씨 전환 확인
- 실시간 날씨, 대기질, 음식 API 성공 및 실패 폴백 확인
- GitHub Pages 주소를 시크릿 창에서 확인
